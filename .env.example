# PostgreSQL connection string
DATABASE_URL=postgresql://postgres@localhost/postgres

# Server port (default: 3333)
# PORT=3333

# Admin dashboard password (required to enable /admin)
ADMIN_PASSWORD=changeme

# Session secret (auto-generated if not set; also used to sign unsubscribe tokens)
# SESSION_SECRET=

# Email notifications (optional — notifications disabled if SMTP_HOST is not set)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=notifications@example.com

# Base URL for links in notification emails (unsubscribe URLs, document links)
# BASE_URL=https://your-remarq-instance.com

# Digest interval in milliseconds (default: 86400000 = 24 hours)
# DIGEST_INTERVAL_MS=86400000
